# TODO

> for the finance notebook, don't define your own ontology, just use the
> right FIBO module.

- [x] find the right FIBO modules for currencies and monetary amounts:
      `FND/Accounting/CurrencyAmount` and `FND/OwnershipAndControl/
      Ownership` were already in the fixtures, which only needed a minimal
      Commons `Collections` stand-in for `comprises`
- [x] rewrite the finance notebook on FIBO's own vocabulary: buckets as
      compound predicates over FIBO classes, the three guarantees proved
      from FIBO's axioms plus the portfolio's own facts
- [WIP] @claude-allegories-2026-09-03 18:30 tests, changelog, validation
      and sign-off
