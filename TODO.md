# TODO

> alright lets forget about uploading the notebooks, they wont work on
> marimohub because of the hermit dependency anyway.
> switch to OWLAPY

- [WIP] @claude-allegories-2026-09-03 21:00 investigate owlapy hands-on:
      reasoners available without a Java runtime, FIBO fixture parsing,
      instance retrieval, subsumption and consistency
- [ ] port `discopy.owl` from `owlready2` to `owlapy`, keeping the
      `Relation`/`Query` API and the deduction-only semantics
- [ ] port the tests and both notebooks, update the `semantic` extra
- [ ] changelog, validation and sign-off
