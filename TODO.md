# TODO

> go for phase 3

Phase 3 of the approved purification plan: the dagger laws join the axiom
matrix, and the `eval(str(x)) == x` idea is settled.

- [ ] `dagger_involution` and `dagger_contravariance` on `abc.Category`, `dagger_monoidality` on `abc.MonoidalCategory`, with `.inapplicable` where there is no dagger
- [ ] Decide `eval(str(x)) == x`: box names generated by the strategies are uuid-based, so probe whether the property is statable at all
- [ ] Delete the unit dagger tests the axioms subsume, guardrail green
- [ ] CHANGELOG updated, TODO deleted
