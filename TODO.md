# TODO

> in discopy.owl, provide a way to load all rules from the loaded knowledge base as
> discopy `Equation`s, and display them all in a cell of the demo notebook.

- [WIP] @claude-allegories-2026-09-02 15:00 `axioms` over a whole `World` in one
      batched HermiT run, with `constructs_of` factored out
- [ ] the rule-book cell of the notebook, every axiom expandable
- [ ] tests at 100% coverage, changelog and sign-off
